# Play-test, 2026-09-06 — triage

Nine reports from an actual play session, with screenshots. What each one turned
out to be, and what is still open.

**The session was played on `dds_dev.exe`, and its trace survived.** The machine
lost power mid-session, but `play/en/ddswin/trace.bin` holds **150,019 complete
20-byte records with nothing torn** — the kernel had flushed all of it. That is
the first time this project has had a real play session on record, and it answers
several of these outright. Anything below citing token counts or character counts
comes from it.

Two things about the dev build matter here. It differs from `dds.exe` by
**exactly the three tracer call-site rel32s and the `.trc` section** — the English
content is identical, so nothing below is a dev-only artefact. But the copy in
`play/en` was built at 03:30 and was **missing every exe string fixed in
8c46197**, which is why items 3, 4 and 6 were real for this player. Both exes are
rebuilt and installed now.

My first theory — that a drifted overlay had stopped serving these files — is
**wrong, and the trace disproves it.** The overlay was working: `m/MS0059` ran
25.4% of its tokens on virtual PCs, `m/MS0017` 28.2%, `m/MS00DD` 14.0%. A virtual
PC is a thing only the overlay produces.

| # | Report | Status |
|---|---|---|
| 1 | Clerk lines not translated | **Re-test.** `m/MS0104`/`MS0100`/`MS0102`/`MS0110` carry 24/22/22/64 spans in the current overlay. My earlier "the clerks are done" was wrong for a different reason: I checked `m/MS0080`, which is the *closed-shop* messages, and generalised from it. |
| 2 | After-battle dialogue grammatically off | **Real, and diagnosed — see "the full-width digits" below.** `m/MS00DD` fetched 14,696 ASCII against 706 double-byte, so it is overwhelmingly English; the 13 rows with no English are pure macros (`{04:05}`, `{03:0B}`) with nothing to translate. |
| 3 | Level-up box untranslated | **Fixed** (8c46197). Exe strings at `0x468AAC`–`0x468B18` and `0x46A400`. |
| 4 | Skill names in battle untranslated | **Fixed** (8c46197). `0x46A5A4` `魔法名称` and the `0x46A5B0` row template. |
| 5 | Exam results mix up 25 and 50 questions | **Open.** Not diagnosed. `m/MS0017` holds the exam; the two numbers suggest one line is being served from a neighbouring span rather than a bad translation, which is the same drift signature as items 1/2/7 — so re-test first, and if it survives, trace the exam route (`routes/`) rather than reading the table. |
| 6 | Most menus untranslated or broken | **Fixed** (8c46197). This was the large one: 99 of the 104 Japanese strings the original exe still addresses are now English. The 5 left are meant to stay — three runs of full-width spaces used to clear a line, and the two kinsoku tables. |
| 7 | Poison dragon fight not translated | **Explained, and fixed by 8c46197.** `m/MS0059` fetched **2,704 ASCII characters and zero double-byte** in that session — the script was already 100% English. What was Japanese on that screen was the battle UI: the skill-list header `魔法名称` and the `%-16.16s  MP  効果` row template, which the 03:30 dev exe did not yet have. Same root cause as item 4. |
| 8 | Translations doubled (Emi, Yuuka) | **Fixed** (2904243). v0.05 stripped the engine's runtime name prints, so a line the engine prints a name *inside* is one line there and two or three spans here; `carry` put the whole English on the first fragment. `make_draft_tree` now refuses a reference that closes (line break or terminal punctuation) over Japanese that ends open, at ≥40 characters and >4× the Japanese — 34 rows. |
| 9 | Flags set for dialogue you did not trigger | **Open.** Not diagnosed, and the one report that cannot be a text bug: the overlay never rewrites a script byte, so no branch, flag or record length can differ between a Japanese and an English run. If this reproduces on the current build it is either original-game behaviour or something outside the interpreter, and the tool for it is the flag-write hook the plan names, not the tables. |

## What the exe scan settled

A pass over `.rdata`/`.data` for NUL-terminated cp932 strings, keeping only
those with two or more Japanese characters that something still addresses,
found 104 in `dds_org.exe`. Worth knowing for next time:

* 46 of the strings that *look* untranslated are the developers' own assertions
  — "臨海コロシアムの防具屋でない場合は連絡して下さい。Takubo" and 45 more like
  it, plus a note signed S.Iseki and one signed きたべ. They print only on paths
  the shipping game does not take. Leave them; they are a record of who wrote
  the shop code.
* Scanning `.text` as well is a waste of time: ordinary x86 decodes as kanji
  constantly, and every hit there was a false positive.
* `et/` has no untranslated text outside the tables. The 2,430 "characters"
  a naive scan finds in 13 `et/` files are binary fields decoding as kanji.


## The full-width digits — the one real defect the trace found

Every double-byte character the whole session fetched — 1,576 of them — came from
**the original file bytes, never from our overlay text**. So no English span
anywhere in that session contained a Japanese character; the corpus is clean on
this. What the player was seeing is narrower and more specific than "the grammar
is off":

> `４７ HP of damage`

**559 of the 858 stray characters in the battle-message files are full-width
digits and full-width spaces.** The engine renders every number the `{03:xx}`
macro expands as zenkaku, which was right for a Japanese game and reads as a
font glitch inside an English sentence. The rest are demon attack names — 甘い息
(Sweet Breath), 体当たり (Tackle) and about 90 other characters — which are **not
stored as plain text in any game file**: a byte search for them across `m/`,
`et/`, `p/`, `fc/` and the exe finds nothing, so they are assembled at runtime
from something the pipeline does not model yet.

Neither is fixed. The digits need the engine's number formatter, which is not a
string edit — there is no `０１２３４５６７８９` table in the image (the only
zenkaku digits in `.data` are the debug menu's own `＋１０`/`－１００` labels), so
it converts arithmetically and the routine has to be found by disassembly. That
makes it a **rendering change**, which is the same category as the two
non-translation exe edits in `docs/exe-patches.md` and is worth agreeing before
doing rather than after.

## Reading a trace, for next time

* A **virtual PC** (`pc0 >= image_end`, and `image_end` comes from the overlay
  directory) is produced by nothing but the overlay, so the share of virtual
  tokens per file is a direct yes/no on whether English was served — no guessing
  from screenshots.
* Reconstructing the character stream from the `ch` field says what was *on
  screen*, which is the question screenshots answer and static coverage numbers
  do not. `m/MS0059` looked untranslated to the player and was 100% English.
* `trace selfcheck` reporting 22,584 of 150,019 records whose bytes "do not match
  the build" is **expected, not an error**, when the trace ran with an overlay and
  the check is against the Japanese `m/`. The mismatch rate tracks the served
  share.
* Two file ids in the trace have no `m/MS####.BIN`: `0x00FF` (2,812 tokens) and
  `0x007F` (470). Both logged `flags=0`, so the interpreter context was *not*
  null and they are not the known stale-context case. Unexplained; not chased.

---

# Second session, same night — five more reports

Played on the rebuilt `dds_dev.exe`, and this trace survived too:
`traces/2026-09-06b-murmur.bin`, 76,515 clean records.

| # | Report | Status |
|---|---|---|
| 1 | Green area text untranslated (`昇り階段`) | **Located, not fixed.** `et/ET0101.BIN` — the map-object label table. 96 records: Terminal, Exit, Up Stairs, Down Stairs, Elevator, Chute, Computer Shop, Weapon Shop, Teleporter, Pharmacy, Cathedral of Shadows, Hospital, Fountain, Recovery Point, Armour Shop, Bar, Rag's Shop, Item Shop. 588 bytes, tiny. |
| 2 | Yuuka's line only half translated | **Open.** The line renders `それより、Emiさんがあんな事になって、Hayasaka is coping with it all...` — the name macros expand to English and the tail span is served, but the leading spans are not. Same span-splitting shape as report 8 from the first session, opposite symptom. |
| 3 | Magic menu untranslated in status | **Located, not fixed.** `et/ET0004.BIN` — the skill/magic database. 309 records, name + description, **5,509 characters of Japanese**. This is also where the previous session's demon attack names (甘い息, 体当たり) come from, so it closes that loose end too. |
| 4 | First Murmur encounter has wrong speaker tags | **Fixed.** See below. |
| 5 | Post-exam-failure scenes play during the Murmur fight | **Not reproduced in the trace.** See below. |

## Why the raw byte searches kept failing

The previous session's writeup said the demon attack names were "not stored as
plain text in any game file". True but misleading, and the reason matters: **the
`.BIN` containers are XOR-obfuscated**, so a byte search of the file on disk finds
nothing even for text that is plainly there. `container.split()` decodes them, and
the same search over `b"".join(c.body for c in conts)` finds everything at once —
`昇り階段` in `m/MS000D`, `m/MS0056` and `et/ET0101`; `円月殺法` and `剣を回転` in
`et/ET0004`. Search decoded bodies, never the file.

## Report 4: the speaker tags — a systematic fix

The line "This is bad. This one is a demon of an absurdly high level... If we
fight it head-on, we don't stand a chance!" was tagged `Murmur:`. It is Nishino's.

The span's Japanese is `{08:04}：` — **a pool macro, not a literal name**. The
engine looks the name up at run time, and the pool word is 西野. v0.05 had written
who it thought was *really* talking instead, and the promoted reference replaced
the macro with the wrong literal.

Grouping every speaker-tag row by its Japanese exposes the whole class: **10,694
speaker-tag rows, 275 distinct tags, 60 of them translated more than one way, 216
rows sitting in a minority spelling.** `{08:03}` (英美, Emi) appears as "Murmur"
4 times and "Kusaka" twice; `{08:00}` (早坂, Hayasaka) as "Murmur" twice;
`{08:04}` (西野, Nishino) as "Murmur" five times; `{08:02}` (山瀬, Yamase) as
"Marduk" eight times.

`tools/make_draft_tree.py` now corrects a promoted reference for a span that is
exactly one pool macro plus the full-width colon, using **the pool table's own
English** as ground truth — 32 rows. Two deliberate limits:

* **Only proper nouns.** Several pool words do double duty as common nouns
  mid-sentence, so `{08:0B}` reads "demon" and a tag spelling it `Demon:` is
  better English than the pool's own lower-case copy.
* **Only where the reference would have been promoted.** All 70 disagreements
  were in `ref_en`; not one was in our own `en` column.

Marduk is the interesting call. In `m/MS0010` the engine prints 山瀬 while Marduk
is speaking through him. A Japanese player sees "Yamase", so an English reader
should too — the reveal is the scene's, not the tag's.

The literal tags are left alone and want a human: `信者` is Cultist ×130 /
Devotee ×62, `若者` is Young Man ×183 / Young Person ×21 / Youngster ×7 / Young
Woman ×5, `平沢博士` is Doctor Hirasawa ×66 / Dr. Hirasawa ×23. Those are
consistency calls, and "Young Woman" may well be deliberate, so majority-wins
would do damage.

## Report 5: traced to a goto, source not identified

Clarified by the player: the report is about the *scenario*, not a literal keycode
prompt. The radio scene — Katsuragi listening while Yamase is wounded — is the
opening of the fail-route sequence that ends with opening the shelter by code, and
it played on a pass route, right after the Murmur battle.

That is in the trace. At record **75031** the engine executes **`ch = 0x0C`,
goto-record**, and `pc` lands on `0x0916`, which the next record's index entry
(`idx_off=0x0916 idx_len=731`) identifies as **`m/MS0001` record 0x01** — the radio
scene. So the jump is real and it is a single, deliberate `0C`, not a fall-through.

**What it is not.** The first thing that looked guilty was the byte run
`1E 10 01 01` sitting at the end of the overlay tail the engine was reading, whose
`01 01` matches "file 1, record 1" exactly. It is a coincidence: `1E 10` is opcode
`0x210`, the `<wait>` token (22,295 uses), and `01 01` are its operands. The run is
present byte-for-byte in the original script at `MS001F+0x25D1`, and our English
tail reproduces it exactly — `...enough.
` + `0a 1e 10 01 01` against the
original's `...一興。
` + `0a 1e 10 01 01`.

**Who jumped is still unknown.** Exactly three sites in the whole game hold
`0C 01 01`: `m/MS0001` record 0x04, `m/MS000B` record 0x00, and `m/MS00AF` record
0x1B. The trace cannot say which fired, because **every locating field on record
75031 is stale**: it reports `file=0x0DD rec=0x00` and an index entry naming
`m/MS001F` record 0, whose runtime range `0x0400..0x0EEC` does not contain the
logged `pc0` of `0x5907` at all. This is the stale-`0x4911B0` case the plan warned
about, and it is worse than "the file id lags" — on a cross-file jump the *source*
of the jump is unrecoverable. **A tracer gap worth closing:** log the `0C`/`0D`
operands, or hook `0x00433E70` (which reads them) rather than relying on
`0x4911B0`.

**The overlay cannot be the cause**, and this is structural rather than a hunch: it
never rewrites a script byte, so no branch, flag or record length can differ
between a Japanese and an English run. Whatever picked that goto picked it from
flag state, and that is the same suspicion as report 9 from the first session.
The tool for it is `trace diff` against a Japanese run of the same route —
`play/jp` and `build_image(english=False)` exist for exactly this — and failing
that, a flag-write hook. Neither is a text problem.

## What the earlier "no keycode text" check actually showed

Nothing in this trace asks for a keycode — no `code`, `番号`, `コード` or
`パスワード` anywhere in the session's text, and the exam file `m/MS0017` ran in
four blocks, all of them ordinary room scenes (waking up, "What will you do?",
"Open terminal", the Yuuka scene). What runs straight after the Murmur battle is
`m/MS0001` record 0x01: Katsuragi listening on the radio, Yamase being wounded,
then a `>What will you do?` with "Contact them" / "Wait and See".

Two possibilities, and the trace cannot separate them yet: the keycode scene came
**before** the 76,515 records this file holds (the tracer opens `trace.bin` fresh
each run, so only the last run survives), or it is original-game behaviour. What
would settle it is a `trace diff` against a Japanese run of the same route — which
is exactly what `play/jp` and `build_image(english=False)` exist for, and is the
plan's own answer for "flow looks wrong".

**Keep the trace after a session that shows something odd.** `trace.bin` is
overwritten on the next launch; copy it into `traces/` before relaunching.


## A negative result worth keeping

Chasing the `m/MS00AF` row whose Japanese is `－１０` but whose English is
"Request" — a plain off-by-one against the next row — a corpus-wide check for
adjacent rows in one record sharing English while their Japanese differs found
**26 rows, and most are correct**: the game writes three or four politeness
variants of the same line for different speakers, and one English translation
serves them all. There is no widespread shifted-block class. The `MS00AF` one is
real but it is in the debug menu.

## Could a translation that is *not* in the overlay have caused report 5?

The right question to ask, and the gap in "the overlay cannot change flow" — which
is true of the overlay and says nothing about the files we rebuild on disk. Every
such change, and whether it can move control flow:

| changed on disk | can it move flow? | this bug |
|---|---|---|
| `m/` story scripts — **0 of 309 changed** | no, byte-identical | **ruled out**, and this is where the goto and everything that could select it live |
| `et/ID*` — **17 script files, sizes changed +25..+156** | **yes, in principle** — records move and branches need relocating | `audit`: 795 branch displacements, no control-flow difference — **and none of the 17 executed in either session** |
| `et/ET0000.BIN` +24 B | race/title data, not script | no |
| `et/et0102.bin` item DB, 72,623 B | loader is bounded: 131,072-byte buffer and a real `cmp eax, ebx / ja .Lclose` per container | no |
| `p/` — **432 files, every one the same size** | the fixed 16-byte name field only, so nothing relocates | no |
| the exe — 1,015 bytes | the only in-place *data* write is `menus.EFFECTS`, 7 bytes at stride 8 with the id byte asserted unchanged | no |

So the goto lives in an unmodified file, everything that could have selected it
lives in unmodified files, and the one category that could theoretically divert
flow never ran. That does not *prove* the sequence is the original game's — it
narrows the cause to flag state, which is report 9 — but it does rule out the
translation.

### The standing risk this question exposed

**17 `et/ID*` files are byte-rebuilt with changed record sizes.** That is precisely
the failure mode behind the project's three earlier blockers — "our opcode model
was incomplete → a relocated or unrelocated branch" — and it is the exact thing the
overlay was built to make impossible. These files bypass it because they are `et/`,
not `m/`.

They are the demon-negotiation scripts, which is why nothing has gone wrong yet:
neither session reached a demon conversation. **When demon talk is first play-tested,
that is where a flow bug would come from,** and `audit` being clean is necessary but
not sufficient — it checks branch displacements, not the engine's own record index.
Bringing `et/ID*` under the overlay, or proving these 17 by `trace diff`, is the
cheap insurance.
